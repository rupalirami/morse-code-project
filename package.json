{
  "name": "morse-code-project",
  "version": "1.0.0",
  "description": "A simple morsecode translator that takes a string as input and translates it into morse code characters. The input takes only alphabetic characters.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-jest": "^29.1.0",
    "jest": "^29.1.1"
  }
}
